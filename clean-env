#!/usr/bin/env bash

# Undo node
n-uninstall -y

# Restore bashrc
mv ~/.bashrc_bak ~/.bashrc
source ~/.bashrc

# Undo Core Install
sudo apt -y curl vim python3-pip build-essential

# Remove orphaned packages
sudo apt -y autoremove
